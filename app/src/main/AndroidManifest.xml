<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.al.happuc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".UI.Activity.Opportunite"></activity>
        <activity android:name=".UI.Activity.Forum" />
        <activity android:name=".UI.Activity.Cours" />
        <activity android:name=".UI.Activity.Bibliotheque" />
        <activity android:name=".UI.Activity.Instutition" />
        <activity android:name=".fragment.AccueilFragment" />
        <activity android:name=".UI.Activity.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>